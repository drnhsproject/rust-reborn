server:
  host: "0.0.0.0"
  port: 8000
  environment: "development"

database:
  url: "postgres://admin:password@localhost:5432/rust_reborn"
  max_connections: 5
  min_connections: 1

jwt:
  secret: "aGRyZWJvcm4="
  expiration_hours: 24
  refresh_expiration_days: 30

media:
  upload_dir: "./uploads"
  max_file_size: 10485760  # 10MB in bytes
  allowed_extensions:
    - "jpg"
    - "jpeg"
    - "png"
    - "gif"
    - "webp"
